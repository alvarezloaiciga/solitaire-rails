column = $("#<%= object_id(@card_mover.origin_column) %>");
card = $("#<%= object_id(@card_mover.origin_card) %>");
<% cards = @card_mover.origin_column.cards_from(@card_mover.origin_card) %>
<% cards.each do |car| %>
  column.append($("#<%= object_id(car) %>"));
<% end %>
$('.move-card-form :input').not(':checkbox, :submit').removeAttr('value');
setDragAndDropAfterState();
